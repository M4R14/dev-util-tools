node_modules
dist
build
coverage
.vite

# Generated by `predev`/`prebuild` (scripts/generate-release-notes.mjs), so its formatting is
# whatever the generator emits. Without this line `npm run format:check` goes red again the next
# time anyone runs a build — a gate that fails on its own side effects teaches people to ignore it.
src/content/blog/auto-release-notes.md
